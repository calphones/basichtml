# ── Core ───────────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.12
Pillow>=10.3
pyyaml>=6.0
python-dotenv>=1.0

# ── Computer Vision ────────────────────────────────────────────────────────
opencv-python>=4.9
torch>=2.2
torchvision>=0.17
onnxruntime-gpu>=1.17        # swap for onnxruntime if no CUDA
segment-anything-2>=0.1      # SAM 2 (Meta)
# depth-anything-v2 loaded via local ONNX checkpoint

# ── Image & Document Parsing ───────────────────────────────────────────────
pymupdf>=1.23                 # PDF → raster (fitz)
pytesseract>=0.3.10
paddlepaddle>=2.6             # optional PaddleOCR backend
paddleocr>=2.7

# ── 3D / BIM ───────────────────────────────────────────────────────────────
ifcopenshell>=0.7
trimesh>=4.3
open3d>=0.18
ezdxf>=1.2                    # DXF parsing
shapely>=2.0                  # 2D polygon operations

# ── Machine Learning extras ────────────────────────────────────────────────
scikit-learn>=1.4
scikit-image>=0.22
transformers>=4.40
sentence-transformers>=2.7    # NLP command embedding

# ── API / Web ──────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.7
aiofiles>=23.2
httpx>=0.27

# ── Colour science ─────────────────────────────────────────────────────────
colormath>=3.0
colour-science>=0.4

# ── Utilities ──────────────────────────────────────────────────────────────
networkx>=3.3                 # room graph
matplotlib>=3.9               # debug visualisation
tqdm>=4.66
loguru>=0.7
